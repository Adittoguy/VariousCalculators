# VariousCalculators
Various types of my created calculators are in this repo 

1. GUIcalc contains 3 Label, 2 TextFields and 4 Buttons in which we can perform 4 differrent operations add, substract, multiply and divide     after performing calculation result will be displayed in Result label.

2. (SocketProgramming) Client2 and Server2 both are connected! Client2 sends the input to add 2 numbers and Server2 performs operation and the result is sent back to Client2 which is displayed in result label.

3. (SocketProgramming) Client3 and Server3 both are connected! Client3 sends the input to add 2 numbers and Server3 performs operation based on the button clicked as there are 4 Buttons to perform differet operations(Add, Substract, Multiply, Divide) and the result is sent back to Client3 which is displayed in result label.

4. MainCalculator is in process.
    